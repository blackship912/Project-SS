# Project:SS — Alpha 0.6.2

地方中古戸建て投資の物件評価・調査・学習ログを行う UI プロトタイプです。
「記録するアプリ」ではなく「使っているうちに記録されるアプリ」をコンセプトに、
物件のスコアリング・調査チェック・写真管理・比較・お気に入りなどを
HTML/CSS/JavaScript のみ（単一ファイル）で実装しています。

**Alpha 0.6.1 はセーブポイントとして別ファイルのまま保持しています。
このリポジトリの `index.html` は Alpha 0.6.2 の実験版です。**

## GitHub Pages での公開方法

1. このリポジトリの内容（`index.html` と `.nojekyll`）をリポジトリのルートに配置します。
2. GitHub の **Settings → Pages** を開きます。
3. **Source** を `Deploy from a branch` に設定し、対象ブランチ（例: `main`）とフォルダ `/ (root)` を選択します。
4. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` でアクセスできるようになります。

`.nojekyll` は GitHub Pages のデフォルトの Jekyll 処理をスキップし、
ファイルをそのまま静的配信させるためのものです。

**iPhone Safari での動作を前提に作られています。** タップ判定・スワイプ・ボトムシートの
挙動は実機の Safari で確認しながら調整しました（PC ブラウザでも一通り操作できます）。

## 最終ファイルの案内

- **ChatGPT などに見せるべき完成ファイル**：`index.html`
  （このファイル1つで全機能が完結する単一HTMLです。ブラウザで直接開いても動作します）
- **GitHub にアップロードすべきファイル**：`index.html` / `.nojekyll` / `README.md` の3つを
  リポジトリのルートにそのまま配置してください。

## Alpha 0.6.2 での変更点（0.6.1 からの差分）

ユーザー目線でのサマリーです。

1. **SSロゴを Design Reference（Ver.1.2 UX Prototype のロゴ）に忠実な形へ再修正**
   直前のやり取りで「盾型」「初期モックの角丸アイコン」といった別方向の修正を試しましたが、
   このチャットで最初に共有した Ver.1.2 UX Prototype 画像の左上にあるロゴ
   ——縦にやや長い六角形・緑単色・白い二重ライン・中央に「SS」——
   を正式な Design Reference として採用し、その形そのものを CSS で高品質に再現しました。
   六角形の頂点比率はリファレンス画像をピクセル単位で解析して割り出しています。
   設定画面からは白ラインの太さ違い（標準ライン／二重ライン強調）を見比べられます。
   **今後、この Ver.1.2 UX Prototype のロゴを Project:SS の Design Reference として扱います。**

2. **クイックリンク（基本・調査・メモ・ログ）を確実に動くよう修正**
   タップすると必ず対応するパネルへ切り替わり、該当エリアまで自動スクロールするようにしました。
   「押したのに何も起きない」状態を解消しています。

3. **横スワイプエリアの上にもドットインジケーターを追加**
   下側だけでなく、クイックリンクの直下にもドットを表示し、
   上下どちらも現在位置に合わせて連動します。「スワイプできる」ことが伝わりやすくなりました。

4. **お気に入り画面から物件詳細へ遷移できることを確認・維持**
   （実装自体は 0.6.1 時点で組み込まれていました。今回改めて動作確認をしています）

5. **写真編集の「保存」ボタンを明確化**
   編集モードのトグルボタンの文言を「編集中」から「保存」に変更し、
   押すと編集内容を確定して閲覧画面に戻るようにしました。

6. **写真タップで確実にフルスクリーン表示されるよう修正**
   iPhone Safari ではドラッグ操作とタップ操作が干渉して `click` イベントが
   発火しないことがあったため、タッチ操作の中で直接タップ判定を行う方式に変更しました。
   フルスクリーン内での写真送り・閉じる・削除・写真変更もあわせて確認しています。

7. **AIアドバイスは3行まで常に表示、超える場合のみ「続きを読む」**

8. **比較画面のAIコメントが途中で切れる問題を修正**
   全文を保持したまま6行までは表示し、それを超える場合だけ「続きを読む」を表示します。
   比較表や「第一候補に設定」ボタンなど、比較機能全体の構成は変更していません。

9. **基本情報編集の入力方法を改善**
   価格・利回り・家賃・築年数・面積・駐車場などの数値項目は、数値だけを入力する欄と、
   単位を選ぶプルダウン（万円・円/月・%・年・㎡・坪・台など）に分離しました。
   候補にない単位や項目は「その他・手入力」で自由に入力できます。
   構造・間取り・用途地域・情報源などもプルダウン選択＋手入力に対応しています。

10. **編集画面のハンドルをタップ／下スワイプで閉じられるように**
    変更がなければそのまま閉じ、変更がある場合は「保存する／破棄して閉じる／キャンセル」を
    選べる確認シートを表示します。

11. **編集画面内の横スワイプを、写真ギャラリーと同じ指に追従する動きに変更**
    ステップ切り替え後は上部の進捗ドットも連動します。

12. **ホーム右上の三本線メニューを実装**
    「Blueprint情報」「Project:SSについて」「Development DNA」「バージョン情報」の
    4項目を簡易メニューとして実装しました。今後の拡張の入口として残しています。

13. **ホームの「注目物件」カードをタップすると、その物件の詳細画面へ遷移**

14. **「AI詳細を見る→」をモーダル表示に変更**
    以前は画面最上部からインライン表示され、上部のナビゲーションと重なって見づらい状態でした。
    今回、画面中央にカードスタイルのモーダルとして表示するようにし、
    背景を暗くしてAIアドバイスに集中できるようにしています。
    右上の✕ボタンと背景タップの両方で閉じられ、閉じると元のスクロール位置に戻ります。
    本文は3行までの折りたたみ表示から「続きを読む」で全文表示に切り替わり、
    長文でもモーダル内をスクロールして読めます。

色味（テーマカラー）の大きな変更は行っていません。比較機能の仕様・構成も維持しています。

## 既知の未対応点（現時点での制約）

- データはブラウザのメモリ上にのみ保持され、リロードすると初期状態（サンプル3物件）に戻ります。
  永続化（サーバー保存やlocalStorageなど）は意図的に未実装です。
- 新規物件登録（＋ボタン）は、現時点では確認ダイアログのみで実際の登録フローは未実装です。
- 「盾ロゴ」の完全な廃止は行っておらず、設定画面のトグルとして選択肢自体は残しています
  （今回の方針転換により見た目は初期モック寄りに修正済みです）。
- 音声入力ボタン（写真メモ）は見た目上のアニメーションのみで、実際の音声認識は行いません。
- 数値入力欄は `inputmode="decimal"` を指定していますが、桁区切りカンマの自動整形などは
  行っていません（カンマ付きで入力してもそのまま保持されます）。

## ファイル構成

```
.
├── index.html   ... アプリ本体（HTML/CSS/JS 全て単一ファイル）
├── .nojekyll    ... GitHub Pages 用設定
└── README.md    ... このファイル
```
